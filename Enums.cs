namespace Snake
{
    public enum SnakeDirection
    {
        Left, 
        Right,
        Up,
        Down,
    };

    public enum GameStatus
    {
        Start,
        Playing,
        Paused,
        GameOver,
        End,
    }
}
